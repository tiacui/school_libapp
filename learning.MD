0, How to get the number of items in items?
```
items = ['pen', 'pencil', 'eraser']
```

1.0, How to change 80 to 85 in scores?
```
scores = [70, 80, 90, 100]
```

1.1), How to get only the first 3 elements (i.e. [10, 15, 20]) from data?
```
data = [5, 10, 15, 20, 25, 30]
```

1.2), How to get the value "d" from the list? use 2 methods.
```
letters = ['a', 'b', 'c', 'd']
```

1,  How to get the value 8?
```
matrix = [
    [1, 2, 3],
    [4, 5, 6],
    [7, 8, 9]
]
```

2, How to get the value 30?
```
data = [10, [20, 30], 40]
```


3, How to get the email "emma@test.com"?
```
user = {
    "id": 101,
    "profile": {
        "name": "Emma",
        "email": "emma@test.com"
    }
}
```


4.1), How to get the student "Lucy"?

```
classroom = {
    "teacher": "Mr Smith",
    "students": ["Tom", "Lucy", "Jack"]
}
```

4.2), how to change the value "Lucy" to "Lucy123"?



5, How to get the number of pages of "Science"?
```
books = [
    {"title": "Math", "pages": 120},
    {"title": "Science", "pages": 200},
    {"title": "History", "pages": 150}
]
```


6, How to change the score 80 to 82?
```
student = {
    "name": "Liam",
    "scores": [75, 80, 85]
}
```

7, How to add "Charlie" to the members list?
```
team = {
    "name": "Red",
    "members": ["Alice", "Bob"]
}
```

8, How to get the value "yellow" using a negative index?
```
colors = [
    ["red", "green"],
    ["blue", "yellow"]
]
```

9, How to get the size of Class B?
```
school = {
    "classes": [
        {"name": "Class A", "size": 25},
        {"name": "Class B", "size": 28}
    ]
}
```

10, How to add a new key "color" with value "blue"?
```
car = {
    "brand": "Toyota",
    "year": 2020
}
```


11, How to print all books one by one in formats like "Math - 120 pages"?
```
books = [
    {"title": "Math", "pages": 120},
    {"title": "Science", "pages": 200},
    {"title": "History", "pages": 150}
]
```


12, How to print the prices of all fruits in format "fruitname - $price", for example, "apple - $2". but can not hardcode fruit name in the code.

```
prices = {
    "apple": 2,
    "banana": 1,
    "orange": 3
}
```
